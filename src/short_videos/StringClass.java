package short_videos;

public class StringClass {
    public static void main(String[] args) {

        String str1 = "Java";

        String str2 = "Java";  //the JVM checks a string pool in a heap memory and when it
                              // finds one and instead of creating a new one returns to the
                              // existing one

    }
}
